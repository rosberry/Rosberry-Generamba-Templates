{% include 'header' %}

#import "{{ prefix }}{{ module_info.name }}CellItemsFactory.h"
#import "{{ prefix }}{{ module_info.name }}BaseCollectionViewCellItem.h"

@implementation {{ prefix }}{{ module_info.name }}CellItemsFactory

- (NSArray *)cellItemsFromObjects:(NSArray *)objects {
    NSMutableArray *cellItems = [NSMutableArray array];
    for (id object in objects) {
        {{ prefix }}{{ module_info.name }}BaseCollectionViewCellItem *cellItem = [[{{ prefix }}{{ module_info.name }}BaseCollectionViewCellItem alloc] init];
        cellItem.object = object;
        [cellItems addObject:cellItem];
    }
    return [NSArray arrayWithArray:cellItems];
}

@end
