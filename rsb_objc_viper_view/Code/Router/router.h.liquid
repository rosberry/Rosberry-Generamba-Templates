{% include 'header' %}

#import "{{ prefix }}{{ module_info.name }}RouterInput.h"
@class RSBTransitionsCoordinator;

@interface {{ prefix }}{{ module_info.name }}Router : NSObject <{{ prefix }}{{ module_info.name }}RouterInput>

@property (nonatomic, weak) id source;
@property (nonatomic) RSBTransitionsCoordinator *transitionsCoordinator;

@end
