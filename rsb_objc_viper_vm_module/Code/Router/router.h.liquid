{% include 'header' %}
{%
if custom_parameters.inherit != nil %}
#import "{{ prefix }}{{ custom_parameters.inherit }}Router.h"{% 
endif %}
#import "{{ prefix }}{{ module_info.name }}RouterInput.h"
@protocol RSBTransitionHandlerProtocol;

@interface {{ prefix }}{{ module_info.name }}Router : {% 
      if custom_parameters.inherit != nil 
      %}{{ prefix }}{{ custom_parameters.inherit }}Router{% 
      else 
      %}NSObject{% 
      endif %} <{{ prefix }}{{ module_info.name }}RouterInput>
{%if custom_parameters.inherit == nil %}
@property (nonatomic, weak) id<RSBTransitionHandlerProtocol> transitionHandler;
{% 
endif %}
@end
