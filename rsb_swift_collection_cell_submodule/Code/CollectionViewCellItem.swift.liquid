{% include 'header' %}

import UIKit
import CollectionViewTools

final class {{ module_info.name }}CollectionViewCellItem: CollectionViewCellItemProtocol {
    
    private let viewModel: {{ module_info.name }}CollectionViewCellModel
    
    var reuseType = ReuseType(cellClass: {{ module_info.name }}CollectionViewCell.self)
    
    init(viewModel: {{ module_info.name }}CollectionViewCellModel) {
        self.viewModel = viewModel
    }
    
    func size(for collectionView: UICollectionView, with layout: UICollectionViewLayout, at indexPath: IndexPath) -> CGSize {
        return .zero
    }
    
    func cell(for collectionView: UICollectionView, at indexPath: IndexPath) -> UICollectionViewCell {
        let cell: {{ module_info.name }}CollectionViewCell = collectionView.dequeueReusableCell()
        return cell
    }
}
