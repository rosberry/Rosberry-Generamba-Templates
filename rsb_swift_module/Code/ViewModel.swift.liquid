{% include 'header' %}

final class {{ prefix }}{{ module_info.name }}ViewModel {

    private let interactor: {{ prefix }}{{ module_info.name }}Interactor
    private let router: {{ prefix }}{{ module_info.name }}Router
    
	// MARK: - Lifecycle

    init(router: {{ prefix }}{{ module_info.name }}Router, interactor: {{ prefix }}{{ module_info.name }}Interactor) {
        self.router = router
        self.interactor = interactor
    }

    func didTriggerViewReadyEvent() {
        
    }
}
