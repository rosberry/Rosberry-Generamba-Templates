{% include 'header' %}

protocol {{ prefix }}{{ module_info.name }}ViewModelOutput: class {
}

final class {{ prefix }}{{ module_info.name }}ViewModel {

    init(state: {{ prefix }}{{ module_info.name }}State, output: {{ prefix }}{{ module_info.name }}ViewModelOutput) {
    }
}
