{% include 'header' %}

import TableViewTools

final class {{ module_info.name }}Factory {

    static func makeSectionItems(cellModels: [{{ module_info.name }}TableViewCellModel]) -> [TableViewSectionItem] {
        let cellItems = cellModels.map({{ module_info.name }}TableViewCellItem.init)
        let sectionItem = TableViewSectionItem(cellItems: cellItems)
        return [sectionItem]
    }
}
