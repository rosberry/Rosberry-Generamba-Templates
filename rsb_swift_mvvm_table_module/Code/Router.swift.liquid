{% include 'header' %}

import Foundation

final class {{ prefix }}{{ module_info.name }}Router {

    private weak var viewController: {{ prefix }}{{ module_info.name }}ViewController?
    private weak var viewModel: {{ prefix }}{{ module_info.name }}ViewModel?

    func set(viewController: {{ prefix }}{{ module_info.name }}ViewController, viewModel: {{ prefix }}{{ module_info.name }}ViewModel) {
        self.viewController = viewController
        self.viewModel = viewModel
    }
}
