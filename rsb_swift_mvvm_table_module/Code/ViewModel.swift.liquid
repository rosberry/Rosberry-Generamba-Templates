{% include 'header' %}

protocol {{ prefix }}{{ module_info.name }}ViewModelDelegate: class {

    func update(animated: Bool)
}

final class {{ prefix }}{{ module_info.name }}ViewModel {

    typealias Dependencies = Any

    weak var delegate: {{ prefix }}{{ module_info.name }}ViewModelDelegate?
    weak var output: {{ prefix }}{{ module_info.name }}ModuleOutput?

    private let dependencies: Dependencies    
    private let router: {{ prefix }}{{ module_info.name }}Router

    init(dependencies: Dependencies, router: {{ prefix }}{{ module_info.name }}Router) {
        self.dependencies = dependencies
        self.router = router
    }

    // MARK: - Events

    func viewDidLoad() {
        delegate?.update(animated: false)
    }
}

// MARK: - {{ prefix }}{{ module_info.name }}Input

extension {{ prefix }}{{ module_info.name }}ViewModel: {{ prefix }}{{ module_info.name }}ModuleInput {
}
