{% include 'header' %}

protocol Has{{ prefix }}{{ module_info.name }} {
    var {{ module_info.name | downcase }}: {{ prefix }}{{ module_info.name }}Protocol { get }
}

protocol {{ prefix }}{{ module_info.name }}Protocol {

}
