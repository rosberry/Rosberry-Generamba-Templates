{% include 'header' %}

final class {{ prefix }}{{ module_info.name }}ModuleBuilder {
    
    static func module(dependencies: {{ prefix }}{{ module_info.name }}Dependencies) -> {{ prefix }}{{ module_info.name }}ViewController {
		let viewController = {{ prefix }}{{ module_info.name }}ViewController()
        let router = {{ prefix }}{{ module_info.name }}Router(viewController: viewController)
        let interactor = {{ prefix }}{{ module_info.name }}Interactor(dependencies: dependencies)        
        let presenter = {{ prefix }}{{ module_info.name }}Presenter(viewController: viewController, interactor: interactor, router: router)        
        viewController.presenter = presenter        
        
        return viewController
    }
}
