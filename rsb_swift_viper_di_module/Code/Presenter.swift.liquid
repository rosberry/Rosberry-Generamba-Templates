{% include 'header' %}

final class {{ prefix }}{{ module_info.name }}Presenter {

    private unowned let viewController: {{ prefix }}{{ module_info.name }}ViewController
    private let interactor: {{ prefix }}{{ module_info.name }}Interactor
    private let router: {{ prefix }}{{ module_info.name }}Router
    
	// MARK: - Lifecycle

    init(viewController: {{ prefix }}{{ module_info.name }}ViewController, interactor: {{ prefix }}{{ module_info.name }}Interactor, router: {{ prefix }}{{ module_info.name }}Router) {
        self.viewController = viewController
        self.router = router
        self.interactor = interactor
    }

    func didTriggerViewReadyEvent() {
        
    }
}
