{% include 'header' %}

final class {{ prefix }}{{ module_info.name }}Router {

    unowned let viewController: {{ prefix }}{{ module_info.name }}ViewController

    init(viewController: {{ prefix }}{{ module_info.name }}ViewController) {
        self.viewController = viewController
    }
}
