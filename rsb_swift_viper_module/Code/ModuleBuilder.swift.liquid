{% include 'header' %}

final class {{ prefix }}{{ module_info.name }}ModuleBuilder {
    
    static func module() -> {{ prefix }}{{ module_info.name }}ViewController {
        let router = {{ prefix }}{{ module_info.name }}Router()
        let interactor = {{ prefix }}{{ module_info.name }}Interactor()
        let viewController = {{ prefix }}{{ module_info.name }}ViewController()
        let presenter = {{ prefix }}{{ module_info.name }}Presenter(view: viewController, interactor: interactor, router: router)
        
        router.transitionHandler = viewController
        viewController.output = presenter
        
        return viewController
    }
}
