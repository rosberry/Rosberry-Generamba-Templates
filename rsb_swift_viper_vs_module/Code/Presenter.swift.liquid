{% include 'header' %}

typealias {{ prefix }}{{ module_info.name }}Dependencies = Any

final class {{ prefix }}{{ module_info.name }}Presenter: StatefulPresenter<{{ prefix }}{{ module_info.name }}ViewState, {{ prefix }}{{ module_info.name }}StateStore>, {{ prefix }}{{ module_info.name }}ModuleInput {

    private let dependencies: {{ prefix }}{{ module_info.name }}Dependencies

    weak var output: {{ prefix }}{{ module_info.name }}ModuleOutput?
    
	// MARK: - Lifecycle

    init(dependencies: {{ prefix }}{{ module_info.name }}Dependencies, stateStore: {{ prefix }}{{ module_info.name }}StateStore) {
        self.dependencies = dependencies
        super.init(stateStore: stateStore)
    }

    override func viewReadyEventTriggered() {
        
    }
}

// MARK: - {{ prefix }}{{ module_info.name }}ModuleInput

extension {{ prefix }}{{ module_info.name }}Presenter: {{ prefix }}{{ module_info.name }}ModuleInput {
	
}
