{% include 'header' %}

typealias {{ prefix }}{{ module_info.name }}Dependencies = Any

final class {{ prefix }}{{ module_info.name }}Presenter: StatefulPresenter<{{ prefix }}{{ module_info.name }}ViewState>, {{ prefix }}{{ module_info.name }}ModuleInput {

    private let dependencies: {{ prefix }}{{ module_info.name }}Dependencies

    weak var output: {{ prefix }}{{ module_info.name }}ModuleOutput?
    
	// MARK: - Lifecycle

    init(dependencies: {{ prefix }}{{ module_info.name }}Dependencies, initialState: {{ prefix }}{{ module_info.name }}ViewState) {
        self.dependencies = dependencies
        super.init(initialState: initialState)
    }

    override func viewReadyEventTriggered() {
        
    }
}
