{% include 'header' %}

#import <XCTest/XCTest.h>
#import <OCMock/OCMock.h>

#import "{{ prefix }}{{ module_info.name }}View.h"

#import "{{ prefix }}{{ module_info.name }}ViewOutput.h"

@interface {{ prefix }}{{ module_info.name }}ViewTests : XCTestCase

@property (nonatomic) {{ prefix }}{{ module_info.name }}View *view;

@property (nonatomic) id mockOutput;

@end

@implementation {{ prefix }}{{ module_info.name }}ViewControllerTests

#pragma mark - Setup environment

- (void)setUp {
    [super setUp];

    self.view = [[{{ prefix }}{{ module_info.name }}View alloc] init];

    self.mockOutput = OCMProtocolMock(@protocol({{ prefix }}{{ module_info.name }}ViewOutput));

    self.view.output = self.mockOutput;
}

- (void)tearDown {
    self.view = nil;

    self.mockOutput = nil;

    [super tearDown];
}

#pragma mark - Lifecycle tests

#pragma mark - interface methods tests

#pragma mark - {{ prefix }}{{ module_info.name }}ViewInput methods tests

@end
