{% include 'header' %}
{%
if custom_parameters.inherit != nil %}
#import "{{ prefix }}{{ custom_parameters.inherit }}Interactor.h"{% 
endif %}
#import "{{ prefix }}{{ module_info.name }}InteractorInput.h"

@interface {{ prefix }}{{ module_info.name }}Interactor : {% 
      if custom_parameters.inherit != nil 
      %}{{ prefix }}{{ custom_parameters.inherit }}Interactor{% 
      else 
      %}NSObject{% 
      endif %} <{{ prefix }}{{ module_info.name }}InteractorInput>

@end
