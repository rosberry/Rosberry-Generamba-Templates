{% include 'header' %}

#import <Foundation/Foundation.h>{%
if custom_parameters.inherit != nil %}
#import "{{ prefix }}{{ custom_parameters.inherit }}RouterInput.h"{% 
endif %}

@protocol {{ prefix }}{{ module_info.name }}RouterInput {% 
      if custom_parameters.inherit != nil 
      %}<{{ prefix }}{{ custom_parameters.inherit }}RouterInput>{% 
      else 
      %}<NSObject>{% 
      endif %}

@end
