{% include 'header' %}
{%
if custom_parameters.inherit != nil %}
#import "{{ prefix }}{{ custom_parameters.inherit }}ViewModel.h"{% 
endif %}
#import "{{ prefix }}{{ module_info.name }}ViewOutput.h"
#import "{{ prefix }}{{ module_info.name }}ModuleInput.h"
{% 
      if custom_parameters.inherit != nil 
      %}#import "{{ prefix }}{{ module_info.name }}InteractorInput.h"
#import "{{ prefix }}{{ module_info.name }}RouterInput.h"{% 
      else 
      %}@protocol {{ prefix }}{{ module_info.name }}InteractorInput;
@protocol {{ prefix }}{{ module_info.name }}RouterInput;{% 
      endif %}

@interface {{ prefix }}{{ module_info.name }}ViewModel : {% 
      if custom_parameters.inherit != nil 
      %}{{ prefix }}{{ custom_parameters.inherit }}ViewModel{% 
      else 
      %}NSObject{% 
      endif %} <{{ prefix }}{{ module_info.name }}ViewOutput, {{ prefix }}{{ module_info.name }}ModuleInput>

@property (nonatomic) id<{{ prefix }}{{ module_info.name }}InteractorInput> interactor;
@property (nonatomic) id<{{ prefix }}{{ module_info.name }}RouterInput> router;

@end
